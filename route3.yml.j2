kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: {{ name }}
  namespace: {{ namespace }}
spec:
  to:
    kind: Service
    name: {{ name }}
    weight: 100
  port:
    targetPort: {{ ssh_service_port }}
  wildcardPolicy: None
