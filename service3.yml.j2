kind: Service
apiVersion: v1
metadata:
  name: {{ name }}
  namespace: {{ namespace }}
spec:
  ports:
    - protocol: TCP
      port: 22
      targetPort: 22
      nodePort: {{ ssh_service_port }}
  type: NodePort
  selector:
    kubevirt.io/domain: {{ name }}
